using UnityEngine;

namespace Sana
{
    public class CameraController : MonoBehaviour
    {
        // OnCameraRotate(InputValue value) 에 따라 CameraPivot 의 local y축을 회전
        // OnCameraTilt(InputValue value) 에 따라 CameraPivot 의 local x축을 회전
        // OnCameraZoom(InputValue value) 에 따라 MainCamera 의 local z축을 이동
        
        
    }
}